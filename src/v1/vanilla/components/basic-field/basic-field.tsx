////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

import { BasicTextInput } from "@/src/v1/vanilla/components/basic-text-input/basic-text-input";
import { BasicFieldTypes } from "./basic-field-types";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export function BasicField({ value, onChangeText, optionalProps = {} }: BasicFieldTypes.Props)
{
  const { textInputProps = {} } = optionalProps
  
  return (
    <BasicTextInput
      optionalProps={{
        textInputProps: {
          value,
          onChangeText,
          ...textInputProps
        }
      }}
    />
  )
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

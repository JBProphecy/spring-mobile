////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

import { JaxTypes } from "../types/master";
import { buildNeutralRGB } from "../utils/color";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export * as App from "./app";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export const Percent = {
  50: "50%",
  100: "100%"
} as const satisfies Record<number, JaxTypes.Percent<number>>

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export const RGB = {
  0: buildNeutralRGB(0),
  8: buildNeutralRGB(8),
  12: buildNeutralRGB(12),
  16: buildNeutralRGB(16),
  24: buildNeutralRGB(24),
  36: buildNeutralRGB(36),
  48: buildNeutralRGB(48),
  128: buildNeutralRGB(128),
  176: buildNeutralRGB(176),
  196: buildNeutralRGB(196),
  208: buildNeutralRGB(208),
} as const satisfies Record<number, JaxTypes.Color.RGB<number, number, number>>

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

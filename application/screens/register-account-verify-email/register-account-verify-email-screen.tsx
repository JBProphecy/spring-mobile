////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

import { ClientRouteDataProps } from "@/api/client/client-route-data-props";
import { NativeStackScreenProps } from "@react-navigation/native-stack";
import { StyleSheet, View } from "react-native";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

type Props = NativeStackScreenProps<ClientRouteDataProps, "RegisterAccountVerifyEmail">

export function RegisterAccountVerifyEmailScreen({ route }: Props)
{
  if (route.params.token == null) { throw new Error("Are ya winnin' son?" )}

  return (
    <View style={styles.container}></View>
  )
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

const styles = StyleSheet.create({
  container: { width: "100%", height: "100%" }
})

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
